import React from 'react';

const ShowSubmittedAssignment = ({submit}) => {
    const {} = submit
    return (
        <div>
            
        </div>
    );
};

export default ShowSubmittedAssignment;